extends(src='views/layout.sgr')
  block(name='content')
    header
      ul
        each(loop='menu of contentful.menu')
          li {{ menu.fields.menutitle }} 
          	ul
          		li {{ menu.fields.desc }} 
